<resources>

    <style name="AppTheme" parent="CommonTheme">
        <item name="android:colorBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primaryDark</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:colorControlHighlight">@color/accentLight</item>
        <item name="android:popupBackground">@android:color/white</item>
    </style>

    <style name="ActionBarTheme" parent="android:ThemeOverlay.Material.ActionBar">
        <item name="android:textColorPrimary">@color/icons</item>
        <item name="android:elevation">@dimen/default_elevation</item>
    </style>

    <style name="RaisedButtonStyle" parent="android:Widget.Material.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="RaisedButtonTheme" parent="android:ThemeOverlay.Material">
        <item name="android:colorControlHighlight">@color/accentDark</item>
        <item name="android:colorButtonNormal">@color/accent</item>
    </style>

    <style name="FlatButtonStyle" parent="android:Widget.Material.Button.Borderless">
        <item name="android:background">?android:attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="FlatButtonTheme" parent="android:ThemeOverlay.Material">
        <item name="android:colorControlHighlight">@color/accentLight</item>
    </style>

</resources>
